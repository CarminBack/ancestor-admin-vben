# 小程序本地接入

接口契约以独立小程序的 `docs/backend-api.md` 为准。

```js
export const API_BASE_URL = 'http://localhost:5320'
```

管理网页运行在5174；小程序不要请求5174或5666。

| 接口 | 用途 |
| --- | --- |
| POST /api/memorial/orders | 创建JS祭祀订单 |
| GET /api/supplies/products | 商品列表，category/page/pageSize |
| GET /api/supplies/products/{id} | 商品规格详情 |
| POST /api/supplies/orders | 创建SP用品订单 |
| GET /api/orders/{orderNo} | 统一订单详情 |
| GET /api/service-config | 后台维护的客服信息 |
| GET /api/memorial-records | 订单号或customerName+deceasedName查询已完成记录 |
| GET /api/memorial/packages | 动态套餐列表，data.items |

成功响应为 `{code:200,message:'success',data:...}`；失败使用对应HTTP状态和code（400/401/404/409/500）。管理员接口保持 `code:0` 和Token鉴权，小程序不要调用 `/api/ancestor/*`。

创建订单可传 `Idempotency-Key`，同一次重试应复用相同值与请求体。后端校验金额与库存，不采信用品前端价格。商品ID及规格应从列表/详情获得，不硬编码示例ID。

后台 `PENDING_PAYMENT` 对应小程序 `pending_service` 或 `pending_payment`；后台准备、封包、焚化及待视频状态对小程序统一为 `processing`。确认付款后为 `paid`，完成后为 `completed`。

本地订单持久化到后端 `data/ancestor/`；修改或重启代码不再清空。旧内存中的订单无法自动恢复，请重新下测试单。

视频支持本地MP4上传和HTTPS链接登记。已完成记录只返回已开放视频，本地媒体返回绝对地址和短期签名。实际线上需将API及媒体部署到HTTPS域名；`https://js.mewinyou.asia` 已部署并通过HTTPS/API验证，真机及线上API_BASE_URL可使用该地址。在微信后台配置request和downloadFile合法域名。

客服配置默认留空，请在管理后台系统设置填写实际信息。无微信支付回调：本地测试使用后台人工确认付款。
