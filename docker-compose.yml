services:
  ancestor-admin:
    image: ${ANCESTOR_IMAGE:?Set immutable image tag}
    container_name: ancestor-admin
    restart: unless-stopped
    ports:
      - '127.0.0.1:8080:80'
    env_file: /opt/ancestor-admin/runtime.env
    volumes:
      - /opt/ancestor-admin/data:/app/data
